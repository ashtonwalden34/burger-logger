<li id='burger'>
	{{!-- {{name}} --}}
	{{!-- Button to change devoured status --}}
	<h3>{{this.burger_name}}</h3>
	{{#if devoured}}
		<button class="change-devoured" data-id="{{this.burger_id}}" data-newDevoured="{{this.devoured}}">Devour</button>
	{{/if}}

	<button class="delete-burger" data-id="{{this.burger_id}}">Delete</button>


	

	{{!-- {{#if devoured}}
		<button class="delete" data-id="{{this.burger_id}}">Delete</button>
	{{/if}} --}}

	{{!-- Delte Button --}}
	{{!-- <button class="delete-burger" data-id="{{this.id}}">Delete Burger</button> --}}
</li>
